stage('Build Docker Image') {
    steps {
        script {
            dockerImage = docker.build("ST2DCE:${env.BUILD_ID}", "-f DockerFile ./")
        }
    }
}

stage('Create container in minikube') {
    steps {
        
    }
}
